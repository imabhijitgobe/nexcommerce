// Prisma schema stub (todos P2-038). Models land wave by wave per
// docs/architecture.md Section 7. datasource + generator only for now.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
